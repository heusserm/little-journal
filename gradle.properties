org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
org.gradle.caching=true
kotlin.code.style=official
android.useAndroidX=true
android.nonTransitiveRClass=true

# The one place the app's version is written. Android, the desktop DMG and the
# iOS Info.plist are all derived from it -- see appVersion in build.gradle.kts
# and the `version` lane in fastlane/Fastfile. `fastlane check` fails if any of
# them has drifted.
littlejournal.version=0.1.0
